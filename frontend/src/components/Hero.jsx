export default function Hero() {
  return (
    <section className="hero">
      <p className="eyebrow">Python measures. TAT interprets. React explains.</p>
      <h1>Semantic Code Reviewer</h1>
      <p>
        Paste code, run the analyzer, inspect the metrics, TAT review
        output, and generate AI prompts for your next refactor.
      </p>
    </section>
  );
}